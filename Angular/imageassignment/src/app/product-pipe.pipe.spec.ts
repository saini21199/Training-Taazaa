import { ProductPipePipe } from './product-pipe.pipe';

describe('ProductPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductPipePipe();
    expect(pipe).toBeTruthy();
  });
});
